-- ═══════════════════════════════════════════════════════════════
-- NIGHTOS migration 017 — customers.name_kana
-- ───────────────────────────────────────────────────────────────
-- 氏名の読み仮名（ひらがな）カラムを追加。
-- 漢字氏名だけでは「ひらがなで検索」が当たらないため、読みを保持して
-- 予測検索のヒット対象にする。名前入力時の IME 変換前かなから自動採取
-- するほか、カルテ／店舗フォームから手動編集も可能。
--
-- 性質:
--   - nullable: 既存行への影響なし（コード側は name_kana ?? null で読む）
--   - 後付け運用可能: 既存顧客は編集時に随時補完
-- ═══════════════════════════════════════════════════════════════

alter table customers add column if not exists name_kana text;
